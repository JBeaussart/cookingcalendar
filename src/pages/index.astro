---
/* src/pages/index.astro */
import LandingLayout from "../layouts/LandingLayout.astro";
import { getServerSession } from "../lib/auth";

const { user } = await getServerSession(Astro.request);

if (user) {
  return Astro.redirect("/planning", 302);
}
---

<LandingLayout title="Cooking Calendar - Planifiez vos repas">
  <div class="min-h-screen bg-sage-50">

    <!-- ================= HERO ================= -->
    <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-14 sm:py-16 text-center">
      <h1 class="text-4xl sm:text-5xl md:text-6xl font-bold text-gray-900 mb-5">
        Planifiez vos repas
        <span class="block text-sage-600">en toute simplicité</span>
      </h1>

      <p class="text-lg sm:text-xl text-gray-700 mb-7 max-w-2xl mx-auto">
        Des repas organisés, moins de stress au quotidien  
        et plus de temps pour profiter de votre famille.
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a
          href="/signup"
          class="w-full sm:w-auto px-8 py-4 bg-sage-600 text-white text-lg font-semibold rounded-xl hover:bg-sage-700 transition-colors"
        >
          Créer mon compte gratuit
        </a>
        <a
          href="/login"
          class="w-full sm:w-auto px-8 py-4 bg-white text-sage-700 text-lg font-semibold rounded-xl border border-sage-200 hover:bg-sage-100 transition-colors"
        >
          Se connecter
        </a>
      </div>
    </section>

    <!-- ================= FEATURES ================= -->
    <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 sm:py-14">
      <div class="grid md:grid-cols-3 gap-8">

        <!-- Planning -->
        <div class="bg-white p-8 rounded-2xl border border-sage-100 shadow-sm">
          <div class="w-12 h-12 bg-sage-100 rounded-xl flex items-center justify-center mb-4">
            <svg class="w-6 h-6 text-sage-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
                d="M8 7V3m8 4V3m-9 8h10M5 21h14
                   a2 2 0 002-2V7
                   a2 2 0 00-2-2H5
                   a2 2 0 00-2 2v12
                   a2 2 0 002 2z" />
            </svg>
          </div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">
            Votre planning de la semaine
          </h3>
          <p class="text-gray-600">
            Une vision claire des repas pour anticiper la semaine sans stress.
          </p>
        </div>

        <!-- Recettes -->
        <div class="bg-white p-8 rounded-2xl border border-sage-100 shadow-sm">
          <div class="w-12 h-12 bg-sage-100 rounded-xl flex items-center justify-center mb-4">
            <svg class="w-6 h-6 text-sage-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
                d="M12 6
                   c-1.5-1-3.5-1.5-5.5-1.5
                   S3 5 3 6.5v11
                   c0-1.5 2-2.5 4.5-2.5
                   s4 .5 5.5 1.5
                   c1.5-1 3.5-1.5 5.5-1.5
                   s4.5 1 4.5 2.5v-11
                   c0-1.5-2-2.5-4.5-2.5
                   S13.5 5 12 6z" />
            </svg>
          </div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">
            Votre carnet de recettes
          </h3>
          <p class="text-gray-600">
            Toutes vos recettes préférées réunies au même endroit.
          </p>
        </div>

        <!-- Courses -->
        <div class="bg-white p-8 rounded-2xl border border-sage-100 shadow-sm">
          <div class="w-12 h-12 bg-sage-100 rounded-xl flex items-center justify-center mb-4">
            <svg class="w-6 h-6 text-sage-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
                d="M9 5H7
                   a2 2 0 00-2 2v12
                   a2 2 0 002 2h10
                   a2 2 0 002-2V7
                   a2 2 0 00-2-2h-2
                   M9 5a2 2 0 002 2h2
                   a2 2 0 002-2" />
            </svg>
          </div>
          <h3 class="text-xl font-semibold text-gray-900 mb-2">
            Votre liste de courses prête
          </h3>
          <p class="text-gray-600">
            Tout ce qu’il faut acheter, sans rien oublier.
          </p>
        </div>

      </div>
    </section>

    <!-- ================= COMMENT ÇA MARCHE ================= -->
    <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 sm:py-14">

      <!-- Desktop -->
      <div class="hidden md:block">
        <div class="text-center mb-10">
          <h2 class="text-4xl font-bold text-gray-900 mb-3">
            Comment ça marche ?
          </h2>
          <p class="text-xl text-gray-600">
            Organisez vos repas en quelques minutes, sans prise de tête.
          </p>
        </div>

        <div class="grid md:grid-cols-3 gap-8 max-w-5xl mx-auto text-center">

          <div class="bg-white p-8 rounded-2xl border border-sage-100 shadow-sm">
            <span class="text-4xl">🍽️</span>
            <h3 class="text-xl font-semibold text-gray-900 mt-4 mb-2">
              Ajoutez vos recettes
            </h3>
            <p class="text-gray-600">
              Enregistrez facilement vos recettes préférées  
              avec leurs ingrédients et étapes.
            </p>
          </div>

          <div class="bg-white p-8 rounded-2xl border border-sage-100 shadow-sm">
            <span class="text-4xl">🗓️</span>
            <h3 class="text-xl font-semibold text-gray-900 mt-4 mb-2">
              Planifiez votre semaine
            </h3>
            <p class="text-gray-600">
              Glissez vos recettes dans le planning  
              selon vos envies et votre emploi du temps.
            </p>
          </div>

          <div class="bg-white p-8 rounded-2xl border border-sage-100 shadow-sm">
            <span class="text-4xl">🛒</span>
            <h3 class="text-xl font-semibold text-gray-900 mt-4 mb-2">
              Faites vos courses
            </h3>
            <p class="text-gray-600">
              Votre liste de courses se génère automatiquement  
              à partir de votre planning.
            </p>
          </div>

        </div>
      </div>

      <!-- Mobile -->
      <div class="md:hidden">
        <h2 class="text-2xl font-bold text-gray-900 mb-5 text-center">
          Comment ça marche ?
        </h2>

        <ul class="space-y-3">
          <li class="bg-white p-4 rounded-xl border border-sage-100 flex items-center gap-3">
            <span class="text-2xl">🍽️</span>
            <span>Ajoutez vos recettes</span>
          </li>
          <li class="bg-white p-4 rounded-xl border border-sage-100 flex items-center gap-3">
            <span class="text-2xl">🗓️</span>
            <span>Planifiez la semaine</span>
          </li>
          <li class="bg-white p-4 rounded-xl border border-sage-100 flex items-center gap-3">
            <span class="text-2xl">🛒</span>
            <span>Faites vos courses</span>
          </li>
        </ul>
      </div>

    </section>

    <!-- ================= PRICING ================= -->
    <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 sm:py-14">
      <div class="text-center mb-10">
        <h2 class="text-4xl font-bold text-gray-900 mb-3">
          Une formule simple, pour toute la famille
        </h2>
        <p class="text-xl text-gray-600">
          Commencez gratuitement, puis passez au Premium quand vous en aurez besoin.
        </p>
      </div>

      <div class="grid gap-8 sm:grid-cols-2 max-w-4xl mx-auto">

        <!-- Premium -->
        <div class="relative bg-sage-600 p-8 rounded-2xl shadow-xl border border-sage-400 flex flex-col order-1 sm:order-2 text-white">
          <div class="absolute -top-4 left-1/2 -translate-x-1/2">
            <span class="bg-amber-100 text-amber-800 text-xs font-semibold px-4 py-1 rounded-full">
              ⭐ Le plus choisi
            </span>
          </div>

          <div class="text-center mb-6">
            <h3 class="text-2xl font-semibold mb-2">Premium</h3>
            <p class="text-4xl font-bold">2,99 €</p>
            <p class="text-sm text-white/80">par mois · sans engagement</p>
          </div>

          <ul class="space-y-3 mb-8">
            <li>✔ Recettes illimitées</li>
            <li>✔ Organisation des repas de réception</li>
            <li>✔ Export PDF</li>
            <li>✔ Support prioritaire</li>
          </ul>

          <a
            href="/signup"
            class="mt-auto block w-full text-center py-4 bg-white text-sage-700 font-semibold rounded-xl"
          >
            Découvrir le Premium
          </a>
        </div>

        <!-- Gratuit -->
        <div class="bg-white p-8 rounded-2xl border border-sage-200 shadow-sm flex flex-col order-2 sm:order-1">
          <div class="text-center mb-6">
            <h3 class="text-2xl font-semibold mb-2">Gratuit</h3>
            <p class="text-4xl font-bold">0 €</p>
            <p class="text-sm text-gray-500">par mois · sans engagement</p>
          </div>

          <ul class="space-y-3 mb-8 text-gray-700">
            <li>✔ Planning hebdomadaire</li>
            <li>✔ Jusqu’à 20 recettes</li>
            <li>✔ Liste de courses automatique</li>
          </ul>

          <a
            href="/signup"
            class="mt-auto block w-full text-center py-4 bg-sage-100 text-sage-700 font-semibold rounded-xl"
          >
            Commencer gratuitement
          </a>
        </div>

      </div>
    </section>

    <!-- ================= CTA FINAL ================= -->
    <section class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-14">
      <div class="bg-sage-500 rounded-3xl p-10 sm:p-12 text-center text-white shadow-lg">
        <h2 class="text-3xl sm:text-4xl font-semibold mb-4">
          Prêt à commencer ?
        </h2>
        <p class="text-lg sm:text-xl mb-7 text-white/90">
          Des repas mieux organisés, sans prise de tête au quotidien.
        </p>
        <a
          href="/signup"
          class="inline-block w-full sm:w-auto px-8 py-4 bg-white text-sage-700 text-lg font-semibold rounded-xl"
        >
          Créer mon compte gratuit
        </a>
      </div>
    </section>

  </div>
</LandingLayout>
